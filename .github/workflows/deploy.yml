name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOSTINGER_HOST || secrets.VPS_HOST || '72.61.151.29' }}
          username: ${{ secrets.HOSTINGER_USER || secrets.VPS_USER }}
          key: ${{ secrets.HOSTINGER_SSH_KEY || secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            set -e
            
            # Hostinger Docker Manager tracks projects inside /docker/
            PROJECT_DIR="/docker/pace_bot"
            
            if [ ! -d "$PROJECT_DIR" ]; then
              echo "📂 Creating directory $PROJECT_DIR for Hostinger Docker Manager"
              mkdir -p /docker/pace_bot
              git clone https://github.com/moizkhan-08/PACERESTAURENTBOT.git /docker/pace_bot
            fi
            
            cd "$PROJECT_DIR"
            echo "📂 Working in $PROJECT_DIR"
            
            # Mark directory as safe for git
            git config --global --add safe.directory "$PROJECT_DIR"
            
            git pull origin main
            echo "✅ Code pulled"
            
            chmod +x deploy.sh
            ./deploy.sh
            echo "🚀 Deployed successfully"
